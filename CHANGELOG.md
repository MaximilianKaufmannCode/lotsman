# Changelog

All notable changes to Лоцман are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

Versioning policy: see [CONTRIBUTING.md](CONTRIBUTING.md#versioning).

## [Unreleased]

## [2.4.2] — 2026-06-30

### Fixed
- **Редактор снова может работать со справочниками и реестром.** У пользователя с ролью **Редактор** в меню был только «Профиль», а страница управления **Компаниями** была закрыта (admin-only гард), хотя бэкенд с 2.3.0 разрешает редактору создавать компании. Теперь:
  - в меню редактора (и наблюдателя) есть пункт **«Реестр»**, а у редактора — ещё и **«Компании»**;
  - страница **«Компании»** доступна редактору: он может **создавать и просматривать** компании; **редактирование и архивирование — только администратор** (колонка действий скрыта для не-админов — зеркалит RBAC бэкенда: `POST /assets` = редактор+админ, `PATCH`/архив = админ);
  - **Типы документов** остаются только у администратора (тип задаёт глобальное расписание уведомлений) — редактор по-прежнему выбирает из готовых типов при создании документа.

> Только фронтенд: бэкенд, схема БД и публичный контракт не менялись (бэкенд уже разрешал редактору создавать компании). PATCH согласно [политике версионирования](CONTRIBUTING.md#versioning) — устранение дефекта доступа.

## [2.4.1] — 2026-06-30

### Fixed
- **В уведомлениях теперь видно, к какой компании относится документ.** Письмо о сроке (и событийные письма) показывали тип, номер, срок и ответственного, но **не саму компанию** — было непонятно, по какому контрагенту нужно действовать. Причина: detail-эндпоинт `GET /api/v1/documents/{id}` (его вызывает notification-service) **не резолвил `asset_name`** — в отличие от списочного эндпоинта. Теперь detail-эндпоинт заполняет `asset_name` (и `type_display_name`), и строка **«Компания»** появляется в блоке деталей письма.

### Changed
- **Компания вынесена и в другие места уведомлений** (по итогам аудита всех шаблонов): добавлена в **тему** писем о сроках (видно прямо в списке входящих — миграция `0010`, обратимая), и в **заголовок** событийных уведомлений, ежедневной сводки и in-app-колокола (`Создан документ: SVC-2026/SRL-03 · ООО «Ромашка»`). Письмо-тело о сроках и календарные события уже содержат компанию — они получают её из того же исправления detail-эндпоинта.

> Без изменения схемы БД и публичного контракта (заполняются уже существующие опциональные поля ответа). PATCH согласно [политике версионирования](CONTRIBUTING.md#versioning) — исправление дефекта подачи информации.

## [2.4.0] — 2026-06-25

### Changed
- **Полная переработка почтовых уведомлений — реальная польза, детализация и дизайн.** Письма о сроках (предупреждение / истекает сегодня / просрочено), событийные уведомления (создан / изменён / назначен / вложение / архив) и ежедневная сводка теперь используют единый брендированный HTML-шаблон со следующим:
  - **цветовая индикация срочности** (🟢 актуально / 🟠 скоро / 🔴 просрочено) — важность считывается мгновенно, до чтения текста (pre-attentive восприятие);
  - **блок «с одного взгляда»** (Компания · Тип документа · № документа · Срок действия · Осталось/Просрочено · Ответственный) — ключевые факты структурированно, без вычитывания из абзаца;
  - **кнопка «Открыть документ»** — прямая ссылка на карточку в реестре (ранее ссылки в письмах о сроках не было вовсе — только текст);
  - **человеческая дата** «15 июля 2026, ср» и **корректное склонение** дней («через 3 дня», «5 дней», «1 день») вместо сырого «2026-07-15 (через 3 дн.)»;
  - **поддержка тёмной темы** (`prefers-color-scheme`), скрытый preheader и **ссылка «Настроить уведомления»** в подвале;
  - **полный текстовый (plain-text) дублёр** для клиентов без HTML.
- **Событийные письма и ежедневная сводка** ранее отправлялись простым текстом — теперь оформлены тем же брендированным шаблоном (детали + кнопка), единообразно с письмами о сроках.

### Added
- Модуль `humanize` (русская дата + склонение дней) и структурированный рендерер писем `render_notification_email` (статус-акцент, блок деталей, единый CTA, тёмная тема). Покрыто юнит-тестами (`test_humanize`, `test_email_html`).

> Контент шаблонов о сроках обновлён обратимой data-миграцией `0009` (без изменения схемы БД; downgrade восстанавливает прежний текст 1-в-1). Каналы Telegram/Dion не затронуты. MINOR согласно [политике версионирования](CONTRIBUTING.md#versioning) — улучшение без поломок и без изменения публичного контракта.

## [2.3.0] — 2026-06-25

### Added
- **Создание Компании и Типа документа прямо из формы добавления документа (issue #5).** Рядом с полями «Компания» и «Тип документа» появилось действие **«+ Создать…»**: открывается компактный модал поверх формы, и после сохранения новый элемент **сразу выбирается** в списке — без ухода со страницы и потери уже заполненных полей. Спроектировано с учётом психологии восприятия: сохранение потока/контекста, узнавание вместо запоминания (авто-выбор), мгновенная обратная связь, прогрессивное раскрытие, разумные дефолты (закон Хика). У Типа — авто-код из названия (транслитерация) и предзаполненное расписание уведомлений.

### Changed
- **RBAC:** `POST /api/v1/assets` теперь доступен **редактору или администратору** (раньше — только admin), чтобы редакторы могли создавать Компании прямо при оформлении документа. Создание **Типа документа — только администратор** (тип задаёт расписание уведомлений и применяется глобально). Редактирование и архивирование Компаний по-прежнему admin-only.

> Без изменения схемы БД и данных. MINOR согласно [политике версионирования](CONTRIBUTING.md#versioning) — новая возможность без поломок.

## [2.2.1] — 2026-06-25

### Fixed
- **Поле ИНН теперь действительно необязательно** — компанию можно создать без ИНН. Поле было задумано необязательным (метка без «*», nullable-колонка с `CHECK (inn IS NULL OR …)`, API-схема с `inn` по умолчанию `null`), но форма отправляла пустую строку `""` вместо `null`, а серверная схема отклоняла её по `min_length=10` → сохранение без ИНН падало ошибкой, и поле было фактически обязательным. Теперь форма шлёт `null` для пустого/пробельного значения, а серверная схема дополнительно трактует `""`/пробелы как «не указан» (защита для любого клиента, включая импорт XLSX). Указанный ИНН по-прежнему валидируется (10/12 цифр + контрольная цифра ФНС).

> Без изменения публичного контракта, схемы БД и данных. PATCH согласно [политике версионирования](CONTRIBUTING.md#versioning) — исправление бага (поле было спроектировано необязательным).

## [2.2.0] — 2026-06-25

### Changed
- **Терминология: «Контрагент» → «Компания» во всём интерфейсе.** Пользовательский термин переименован на всех экранах, в формах, фильтрах, подсказках, тостах, закреплённой колонке реестра, заголовках XLSX-экспорта, текстах уведомлений/календаря и в руководстве пользователя (RU + EN: Counterparty → Company). Учтён женский род: согласования прилагательных, причастий и местоимений обновлены («Компания добавлена / обновлена / архивирована», «будет архивирована вместе с ней» и т. п.).

> Меняется только отображаемый текст. Внутренние идентификаторы (`asset`, `asset_id`, `asset_name`), API-контракты, ключи i18n и схема БД — без изменений; данные не затронуты. Импорт XLSX по-прежнему принимает **оба** заголовка — «Компания» и «Контрагент» (старые шаблоны продолжают работать). **MINOR** согласно [политике версионирования](CONTRIBUTING.md#versioning): это запрошенное пользователями изменение продукта без поломок (PATCH зарезервирован за исправлениями, не за продуктовыми изменениями); публичный контракт не меняется.

## [2.1.0] — 2026-06-25

### Added
- **Размер шрифта интерфейса — персональная настройка пользователя.** В разделе «Профиль» появилась карточка «Размер шрифта» с четырьмя пресетами (Компактный / Обычный / Крупный / Очень крупный): выбор применяется мгновенно, есть живой предпросмотр и кнопка сброса. Масштабирование сделано через одну CSS-переменную на корне (`--app-font-scale`, `html { font-size: calc(16px * var(--app-font-scale)) }`) — пропорционально растут и текст, и rem-отступы, складываясь с зумом браузера (WCAG 1.4.4). По умолчанию «Обычный» (100%) — рендер идентичен прежнему. Высота строк виртуализированного реестра теперь зависит от масштаба (нет обрезки строк при крупном шрифте). Управление — сегмент-радиогруппа с клавиатурной моделью WAI-ARIA APG (стрелки / Home / End) и анти-FOUC применением до первого рендера (по образцу переключателя темы).
- **Хранение «для конкретного пользователя» (гибрид).** Источник истины — новое поле `auth.users.ui_font_scale` (проценты 80–150, по умолчанию 100), встроенное в существующие `GET/PATCH /api/v1/auth/me` (без новых эндпоинтов и таблиц); localStorage служит write-through-кэшем для мгновенного применения. Настройка переживает смену устройства/браузера и синхронизируется для пользователя.

### Database
- **auth-service, миграция `0009_add_user_ui_font_scale`:** аддитивное `ALTER TABLE auth.users ADD COLUMN ui_font_scale SMALLINT NOT NULL DEFAULT 100` + `CHECK (ui_font_scale BETWEEN 80 AND 150)`. Изменение метаданных на PostgreSQL 11+ (без перезаписи таблицы, короткий lock), обратимо (`downgrade` снимает constraint и колонку), существующие данные сохраняются, все текущие пользователи получают значение по умолчанию (= прежний вид).

> Обратная совместимость: контракт `/api/v1/auth/me` расширен опциональным полем; событие `auth.user.profile_updated.v1` теперь эмитится только при фактическом изменении ФИО (правка только размера шрифта события не порождает). MINOR согласно [политике версионирования](CONTRIBUTING.md#versioning).

## [2.0.1] — 2026-06-04

### Security
- **web-bff: верификация access-JWT теперь fail-closed.** Подпись RS256 проверяется всегда; без публичного ключа сервис не стартует, если явно не задан dev-opt-in `JWT_ALLOW_UNVERIFIED=true`. Раньше при отсутствии ключа токен декодировался без проверки подписи — потенциальный обход аутентификации при мисконфигурации (CWE-287/303).
- **registry: импорт-сессии xlsx в Redis — msgpack вместо pickle.** Убран риск выполнения кода при десериализации недоверенных данных (CWE-502). Точность типов (Decimal/datetime/date/time/unicode) сохраняется; на один релиз оставлено чтение legacy-pickle (dual-read).
- **Обновлены уязвимые зависимости (SCA):** `cryptography` 44.0.3 → 46.0.7, `PyJWT` 2.12.1 → 2.13.0, `urllib3` 2.6.3 → 2.7.0, `idna` 3.13 → 3.18, `ws` (web) 8.20.0 → 8.21.0. `starlette`→1.0 (мажор) и `vitest`→4 (dev-only) отложены и отслеживаются.
- **nginx:** security-заголовки сохраняются во всех `location` (replace-семантика `add_header`); HTTP→HTTPS-редирект через `$server_name` вместо клиентского `$host` (анти host-header/open-redirect).

### Added
- Per-PR security-гейт в CI (gitleaks/osv-scanner/trivy-config/semgrep, fail на High/Critical) + `HEALTHCHECK` во всех Docker-образах.

> Контракты между сервисами и публичный API не менялись — PATCH согласно [политике версионирования](CONTRIBUTING.md#versioning).

## [2.0.0] — 2026-06-04

### Changed
- **Лицензия — Business Source License 1.1 (BUSL-1.1).** Лоцман распространяется как **source-available** (не OSI-open-source). Production-использование разрешено **кроме** предложения Лоцмана третьим лицам как конкурирующего хостинг-сервиса или коммерческого продукта (Additional Use Grant). Каждая версия автоматически переходит под **MPL-2.0** не позднее четвёртой годовщины выпуска (Change Date; для 2.0.0 — 2030-06-04). Текст MPL-2.0 включён как `LICENSE-MPL-2.0.txt` (будущая Change License). Подробности — [LICENSING.md](LICENSING.md).

> Функциональных изменений кода в 2.0.0 нет.

## [1.29.9] — 2026-06-03

### Added
- Подпись автора в футере: иконка GitHub + «Maximilian Kaufmann» со ссылкой на репозиторий. Размещена по центру тонкого футера как ненавязчивая подпись; на узких экранах скрывается вместе с копирайтом. Внешняя ссылка с `aria-label` и focus-visible, иконка — встроенный SVG (без новых зависимостей). Только вёрстка.

## [1.29.8] — 2026-06-03

### Fixed
- Реестр: при длинном списке шапка таблицы и нижняя панель (итоги и пагинация) уходили из зоны видимости — прокручивался весь документ. Корневой каркас приведён к высоте вьюпорта (`h-dvh` + `min-h-0` по всей flex-цепочке, прокрутка вынесена в контент): шапка залипает сверху, панель итогов/пагинации закреплена снизу, прокручивается только тело списка, а футер с версией всегда виден. Только вёрстка — без изменений данных и API.

## [1.29.7] — 2026-06-03

### Fixed
- Периодическая ошибка «Не удалось загрузить реестр» из-за истёкшего access-токена: SPA не обновлял токен (проактивный планировщик не был подключён, а восстановление по 401 не срабатывало). Теперь при 401 токен прозрачно обновляется и запрос автоматически повторяется; добавлено проактивное обновление до истечения и восстановление при возврате на вкладку / восстановлении сети.

## [1.29.6] — 2026-06-02

### Fixed
- Действия «Заблокировать», «Разблокировать» и «Завершить все сессии» в управлении пользователями возвращали ошибку и не выполнялись — приведены к корректным маршрутам.

### Changed
- Набор действий в меню и карточке пользователя теперь зависит от статуса учётки: для деактивированной доступны только «Активировать» и «Удалить», у системных учёток управление скрыто.

## [1.29.5] — 2026-06-02

### Fixed
- Деактивация, удаление и понижение роли пользователя падали с ошибкой из-за неверного запроса в гварде «последнего администратора». Проверка переписана, защита от гонок сохранена.

## [1.29.4] — 2026-06-02

### Fixed
- Подтверждение чувствительных admin-действий кодом TOTP завершалось ошибкой. Исправлена передача кода — снова работают смена роли, сброс пароля/TOTP, блокировка, завершение сессий, деактивация и активация.

## [1.29.3] — 2026-06-02

### Fixed
- Статус пользователя в списке и в карточке мог различаться (приглашённый показывался как активный). Введён единый расчёт статуса: Системная / Деактивирован / Заблокирован / Ожидает активации / Активен.

### Added
- Фильтр-чип «Системные» и отдельный статус «Системная» для служебных учёток.

## [1.29.2] — 2026-06-02

### Fixed
- Ссылки во всех письмах (приглашение, повторное приглашение, тестовое) теперь берутся из единой настройки `WEB_BFF_URL`. Смена домена больше не требует правок кода.

## [1.29.1] — 2026-06-02

### Added
- Карточка пользователя стала полноценным центром управления: на вкладке «Профиль» добавлены сгруппированные действия (доступ, безопасность, опасные действия). Раньше они были доступны только из меню строки таблицы.

## [1.29.0] — 2026-06-02

### Added
- Действия «Активировать» и «Удалить» пользователя. Удаление выполняет мягкое удаление с сохранением строки для целостности аудита и ссылок документов.

### Changed
- Деактивация пользователя стала обратимой: учётка остаётся в списке со статусом «Деактивирован» и может быть активирована.
- Системные служебные учётки защищены от деактивации, активации и удаления.

## [1.28.1] — 2026-06-02

### Fixed
- Устранено дублирование in-app уведомлений при повторной доставке событий.
- Коалесценция правок документа больше не теряет изменения в новом окне.
- Мгновенные событийные письма и изменения настроек уведомлений теперь фиксируются (в истории доставок и в аудите).

## [1.28.0] — 2026-06-02

### Added
- In-app центр уведомлений: колокольчик в шапке с бейджем непрочитанного, лента уведомлений, отметка «прочитано» и «прочитать всё».
- Клик по уведомлению (и ссылка из письма) открывает карточку соответствующего документа.

## [1.27.0] — 2026-06-02

### Added
- Уведомления о действиях с документами (создание, изменение полей, вложения, назначение ответственным, архивирование) для всех активных пользователей.
- Несколько правок одного документа за короткое окно объединяются в одно уведомление.
- Каналы доставки: in-app лента и email (мгновенно, ежедневной сводкой или выключено — по настройке пользователя); добавлен ежедневный дайджест.
- В профиле — матрица категорий и каналов уведомлений плюс опция «не уведомлять о моих действиях».

## [1.26.0] — 2026-06-02

### Added
- Дедлайн-напоминания теперь получают все активные пользователи, а не только ответственный за документ (системные учётки исключаются).
- Персональные настройки уведомлений в профиле: общий выключатель и тумблер «Сроки актуализации — на почту».

## [1.25.9] — 2026-06-01

### Fixed
- В карточке документа бейдж статуса всегда показывал зелёный «ОК». Теперь статус вычисляется из срока действия так же, как в таблице реестра.

## [1.25.8] — 2026-06-01

### Removed
- Из верхней панели убрана нерабочая дублирующая строка поиска, сбивавшая с толку. Рабочий поиск реестра не затронут.

## [1.25.7] — 2026-06-01

### Fixed
- Ссылка «Открыть документ» в письмах вела на недоступный адрес — теперь используется публичный адрес системы.
- Устранено задвоение кнопки и подписи «Открыть документ» в письмах.

## [1.25.6] — 2026-05-27

### Fixed
- Фильтр «— Не задано» в колонке «№ документа» возвращал пустой результат. Добавлен корректный отбор документов без номера.

## [1.25.5] — 2026-05-27

### Fixed
- В фильтре колонки «Статус» можно было выбрать только одно значение. Теперь фильтр множественный (например, «Скоро» и «Просрочено» одновременно).

## [1.25.4] — 2026-05-27

### Fixed
- Вложения скачивались как HTML-страница и не открывались. Добавлена корректная отдача файлов: PDF и изображения открываются в браузере, офисные форматы скачиваются с правильным именем и типом.

## [1.25.3] — 2026-05-27

### Fixed
- Архивные документы не отображались при включении соответствующего фильтра. Выбор статуса «В архиве» теперь корректно показывает архив.

### Added
- После архивации документа в уведомлении появляется кнопка «Показать архив».

## [1.25.2] — 2026-05-27

### Fixed
- В тёмной теме выпадающие списки (`<select>`) показывали белый текст на светлом фоне. Опции теперь читаемы во всех формах.

## [1.25.1] — 2026-05-27

### Fixed
- Вкладка «История изменений» показывала пусто сразу после сохранения. Добавлено обновление данных при открытии вкладки и кнопка «Обновить» с подсказкой о небольшой задержке.

## [1.25.0] — 2026-05-27

### Added
- Полное редактирование документа из карточки: теперь можно менять все поля, включая контрагента, тип документа и дополнительные поля (раньше — только номер, даты и заметки).
- При смене типа документа поля, отсутствующие в новом типе, удаляются; изменение фиксируется в истории.

## [1.24.17] — 2026-05-27

### Added
- Фильтр по диапазону дат для всех колонок типа «дата», включая дополнительные поля. Новые date-поля получают такой фильтр автоматически, без обновления кода.

## [1.24.16] — 2026-05-26

### Fixed
- Клик по чекбоксу в фильтре колонки случайно срабатывал как сортировка и сбрасывал фильтр для сортируемых колонок. Исправлено.

## [1.24.12] — 2026-05-26

### Fixed
- Многозначные фильтры реестра (контрагент, срок действия, тип, ответственный) не доходили до сервера. Значения фильтров теперь корректно передаются в адресной строке.

## [1.24.11] — 2026-05-26

### Fixed
- Множественные значения фильтров терялись при формировании URL. Затронуты все фильтры реестра с выбором нескольких значений — теперь они применяются.

## [1.24.10] — 2026-05-26

### Fixed
- Прокси-слой схлопывал повторяющиеся параметры запроса до одного значения, ломая все многозначные фильтры реестра. Теперь все значения сохраняются.

## [1.24.9] — 2026-05-26

### Changed
- Фильтр колонки «Действ. до» переведён на список конкретных дат с чекбоксами и опцией «— Не задано» (бессрочные) — единообразно с другими колонками.

## [1.24.8] — 2026-05-26

### Fixed
- Параметры фильтров в адресной строке записывались с лишними кавычками и теряли булевы значения. Сериализация URL приведена к простому виду.

## [1.24.7] — 2026-05-26

### Fixed
- Фильтр «Только бессрочные» не срабатывал, а номер страницы попадал в URL в искажённом виде. Формирование адреса исправлено.

## [1.24.6] — 2026-05-26

### Added
- В фильтре колонок текстовых дополнительных полей появилась опция «— Не задано» для отбора документов с пустым значением.

## [1.24.5] — 2026-05-26

### Fixed
- Фильтр колонки «Статус» отбирал по внутреннему значению БД, а не по тому, что видит пользователь. Теперь опции совпадают с бейджами «ОК / Скоро / Просрочено / Архив».

## [1.24.4] — 2026-05-26

### Removed
- Убран дублирующий боковой фильтр «Статус жизненного цикла контрагента», который путали с колонкой «Активность». Фильтрация по «Активности» осталась через воронку в шапке колонки.

## [1.24.3] — 2026-05-26

### Fixed
- Выбранные фильтры реестра пропадали из адресной строки и не применялись. Маршрутизатор больше не отбрасывает параметры фильтров — фильтрация работает.

## [1.24.2] — 2026-05-26

### Fixed
- Несколько боковых фильтров (статус контрагента, юрисдикция, ИНН, № документа) не доходили до сервера. Привязки исправлены.

### Added
- Фильтр по подстроке ИНН контрагента.

## [1.24.1] — 2026-05-26

### Fixed
- Боковой фильтр по статусу контрагента не действовал. Добавлена его серверная поддержка.

### Changed
- Контрол переименован в «Статус жизненного цикла контрагента» с подсказкой, чтобы не путать с колонкой «Активность» (дополнительное поле документа).

## [1.24.0] — 2026-05-26

### Added
- Фильтры в шапке каждой колонки реестра: воронка с типовыми контролами под каждый вид данных (текст, дата, диапазон, контрагент, тип, ответственный, перечисление), значения подгружаются с сервера.

## [1.23.0] — 2026-05-26

### Added
- Расширенные многокритериальные фильтры реестра: контрагенты, типы, ответственные, диапазоны дат истечения и обновления, статус и дополнительные поля.
- Жизненный цикл контрагента (действующий / в ликвидации / закрыт) с переходом статуса; архивирование контрагента каскадно архивирует его документы.
- Сохранённые наборы фильтров в профиле пользователя (до 20 шт., с пресетом по умолчанию).

## [1.22.0] — 2026-05-26

### Added
- Самопроверка email-канала: в профиле появилась кнопка «Отправить тестовое письмо» с ограничением одно письмо в минуту. Адрес получателя берётся с сервера.

## [1.21.0] — 2026-05-25

### Added
- HTML-оформление писем-напоминаний: брендированная карточка «Лоцман», форматирование текста и кнопка «Открыть документ». Для почтовых клиентов без HTML сохраняется текстовый вариант.

## [1.20.0] — 2026-05-25

### Added
- Метрики Prometheus для пайплайна напоминаний (отправки, длительность, переключения на резервный канал, запуски и пропуски расписания).
- Admin-страница «История уведомлений» с фильтрами по статусу, типу, каналу, документу, пользователю и датам.

## [1.19.1] — 2026-05-25

### Fixed
- Из рассылки напоминаний явно исключены архивные документы и деактивированные пользователи.

## [1.19.0] — 2026-05-25

### Changed
- Шаблоны писем-напоминаний обогащены: тип документа, контрагент, дата истечения, ответственный и ссылка на документ.

### Fixed
- Исправлено формирование имени темы (топика) событий уведомлений, из-за которого часть событий не попадала в аудит.
- Каждому фоновому обработчику задач назначена своя очередь — задачи разных сервисов больше не пересекаются.

## [1.18.0] — 2026-05-25

### Added
- Email-напоминания по документам: предварительное уведомление (за N дней), в день истечения и повторные напоминания о просрочке. Запускаются по ежедневному расписанию; повторные отправки защищены от дублей.

## [1.17.0] — 2026-05-22

### Added
- Администратор может редактировать ФИО пользователя из карточки в разделе «Пользователи» (с подтверждением TOTP). Email через интерфейс не меняется. Изменение фиксируется в истории.

## [1.16.0] — 2026-05-22

### Fixed
- Восстановлена работа admin-действий из веб-интерфейса: смена роли, деактивация, блокировка/разблокировка, сброс TOTP и пароля, завершение всех сессий (ранее падали с ошибкой).

### Added
- Поиск и фильтрация в разделе «Пользователи» по роли и статусу, со счётчиком найденных и кнопкой сброса.

## [1.15.0] — 2026-05-22

### Added
- Карточка пользователя (боковая панель) с тремя вкладками: «Профиль», «Сессии» и «История», а также баннер восстановления деактивированной учётки.
- Восстановление деактивированного пользователя и отзыв отдельной сессии (с подтверждением TOTP).

## [1.14.0] — 2026-05-22

### Fixed
- Восстановлена запись событий в журнал аудита: из-за рассогласования имён потоков большая часть событий авторизации и реестра не попадала в `audit.events`. Подписки исправлены, ранее пропущенные события восстановлены.

### Added
- ADR-0009 (снижение привилегий сервиса system-control) и ADR-0010 (ротация внутренних JWT-ключей) — проектные документы.
- Документы аудита безопасности и плана устранения замечаний.

## [1.13.20] — 2026-05-21

### Changed
- Кнопка «Редактировать» в карточке документа перенесена в шапку (ближе к пользователю на широких экранах); добавлена горячая клавиша «E» для входа в режим правки.

## [1.13.19] — 2026-05-21

### Changed
- Версия в футере читается из публикуемого `version.json` во время выполнения и всегда соответствует текущей сборке.

## [1.13.18] — 2026-05-21

### Fixed
- Реальные файлы `.xlsx` и `.docx` из Excel/Word отклонялись при загрузке. Определение типа файла дополнено разбором структуры архива; решение по-прежнему опирается на содержимое, а не на расширение.

## [1.13.17] — 2026-05-21

### Fixed
- Увеличено окно анализа содержимого при загрузке вложений, чтобы корректно распознавать офисные документы.

## [1.13.16] — 2026-05-21

### Changed
- Вкладка «История изменений» переработана: ФИО автора, человекочитаемые названия полей и значений, группировка по дням и иконки событий.

## [1.13.15] — 2026-05-21

### Fixed
- Вкладка «История изменений» и экспорт в XLSX возвращали ошибку из-за несовпадения адресов API. Добавлены недостающие маршруты.

## [1.13.14] — 2026-05-20

### Fixed
- События не записывались в `audit.events`: обработчик журнала аудита не выполнял вставку. Запись событий с защитой от дублей реализована, ранее накопленные события обработаны.

## [1.13.13] — 2026-05-20

### Fixed
- «История изменений» документа была пустой из-за неверного адреса сервиса аудита и незапущенного обработчика очереди реестра. Адрес исправлен, обработчик запущен.

## [1.13.12] — 2026-05-20

### Fixed
- Получение списка вложений документа возвращало ошибку. Добавлен соответствующий маршрут — список вложений снова доступен.

## [1.13.11] — 2026-05-20

### Fixed
- Сохранение изменений документа (inline-правка и сохранение из карточки) завершалось ошибкой. Сервер теперь принимает изменение только нужных полей; каждое изменение фиксируется в истории.

## [1.13.10] — 2026-05-20

### Fixed
- Завершающий шаг первого входа (обязательная смена пароля) завершался ошибкой авторизации. Поток первичной регистрации доведён до конца.

## [1.13.9] — 2026-05-20

### Fixed
- Подтверждение привязки TOTP и смена пароля на первом входе падали с внутренней ошибкой. Исправлено.

## [1.13.8] — 2026-05-20

### Fixed
- QR-код привязки TOTP искажался при защите от canvas-fingerprinting в браузере. Код теперь рисуется как SVG и читается authenticator-приложением.

## [1.13.7] — 2026-05-20

### Fixed
- QR-код привязки TOTP отображался полосами при включённом Dark Reader или системных цветовых фильтрах. Добавлена защита от инверсии цветов.

## [1.13.6] — 2026-05-20

### Fixed
- На шаге привязки TOTP терялся токен регистрации, из-за чего запрос уходил без авторизации. Поток первого входа исправлен.

## [1.13.5] — 2026-05-20

### Fixed
- Запрос на привязку TOTP без тела отклонялся с ошибкой валидации. Тело сделано необязательным — шаг первого входа работает.

## [1.13.4] — 2026-05-20

### Fixed
- На странице первого входа больше не запрашивается повторный ввод email и одноразового пароля после перехода со страницы входа.

## [1.13.3] — 2026-05-20

### Fixed
- После ввода email и одноразового пароля пользователь не перенаправлялся на страницу первичной регистрации. Переход на `/first-login` добавлен.

## [1.13.2] — 2026-05-20

### Fixed
- Приглашённые пользователи не могли завершить первичную регистрацию (привязка TOTP и смена пароля всегда отклонялись). Поток первого входа полностью переработан и снова работает.

### Security
- Разделены области применения тикетов входа и регистрации — токен одного сценария нельзя использовать в другом.
- Ограничение числа попыток подтверждения TOTP по одному тикету; единый ответ при неверном тикете без раскрытия деталей.
- Маскирование чувствительных полей в логах сделано рекурсивным.

## [1.13.1] — 2026-05-19

### Fixed
- Приглашённые пользователи и учётки после сброса TOTP зависали на бесконечной загрузке при входе. Ответ шлюза приведён к ожидаемому контракту.

## [1.13.0] — 2026-05-14

### Added
- Отправка приветственного письма с одноразовым кодом при создании пользователя с автоматической доставкой. Реализован полный путь доставки приглашений между сервисами.

### Fixed
- Исправлен запрос в диспетчере исходящих событий, из-за которого рассылка событий не работала.

### Notes
- Канал приглашений: обрабатывается email; Telegram и Dion будут добавлены отдельной функцией.

## [1.12.0] — 2026-05-14

### Added
- Персональная ICS-ссылка на календарь для каждого подписчика: на странице подписок показывается ссылка для Outlook с кнопкой копирования и пошаговой инструкцией. Работает независимо от Exchange и масштабируется без участия IT.

### Changed
- Кнопки повторной выдачи прав и ручной отметки доступа убраны из интерфейса (для ICS они не нужны).

## [1.11.2] — 2026-05-14

### Fixed
- Повышена надёжность выдачи прав на общий календарь через Exchange (EWS): обновление прав ограничено только нужным полем, флаги доступа задаются явно.

### Added
- Кнопка «Отметить как выданное» для строк подписчиков — на случай, когда права на календарь выданы средствами IT вне системы.

## [1.11.1] — 2026-05-13

### Added
- Кнопка «Повторить выдачу прав» для строк подписчиков со статусом «не выдано» / «ошибка».

### Changed
- Для выдачи прав на общий календарь через Exchange используется режим impersonation (требует разовой настройки роли на стороне Exchange).

## [1.11.0] — 2026-05-13

### Added
- Переименование колонок реестра и смена типа дополнительных полей (для администратора, на уровне организации). Смена типа требует подтверждения TOTP и блокируется, если поле уже содержит данные.

### Changed
- Заголовки колонок учитывают пользовательские названия: переопределение имеет приоритет над встроенным заголовком.

## [1.10.4] — 2026-05-13

### Added
- Кнопка «Показать все» в панели «Колонки» — включает видимость всех колонок, включая дополнительные поля. Кнопка сброса переименована в «По умолчанию».

## [1.10.3] — 2026-05-13

### Fixed
- Импорт падал с ошибкой на строках с датами в дополнительных полях. Даты теперь сохраняются в строковом формате.

## [1.10.2] — 2026-05-13

### Fixed
- Колонка «Контрагент» отображалась как закреплённая, даже когда администратор закрепил другую колонку. Закрепление теперь управляется единым механизмом.

## [1.10.1] — 2026-05-13

### Added
- При смене закреплённой колонки показывается уведомление об успешном изменении.

## [1.10.0] — 2026-05-13

### Added
- Администратор может закрепить любую колонку реестра слева (а не только «Контрагент»). Выбор сохраняется на уровне организации и применяется ко всем пользователям.

## [1.9.3] — 2026-05-13

### Fixed
- Панель «Колонки» обрезалась по краю страницы. Теперь она отображается поверх интерфейса и не клипается.

## [1.9.2] — 2026-05-13

### Fixed
- Высота панели «Колонки» масштабируется по высоте экрана, список колонок при необходимости прокручивается.

## [1.9.1] — 2026-05-13

### Fixed
- Список колонок в панели «Колонки» больше не обрезается после нескольких строк.

## [1.9.0] — 2026-05-13

### Added
- Перестановка колонок реестра администратором (перетаскиванием или кнопками, с поддержкой клавиатуры). Порядок сохраняется на уровне организации и применяется ко всем пользователям. Колонка «Контрагент» закреплена слева.

### Changed
- Кнопка «Вернуть колонки по умолчанию» сбрасывает и видимость, и порядок колонок.

## [1.8.5] — 2026-05-13

### Fixed
- При импорте значения дополнительных полей могли не сохраняться из-за устаревшей схемы типа. Схема перечитывается после добавления полей — значения сохраняются.

## [1.8.4] — 2026-05-13

### Fixed
- Импорт падал с ошибкой при включении опции «Применить ко всем типам». Исправлено.

## [1.8.3] — 2026-05-13

### Fixed
- При импорте известное дополнительное поле могло терять значения для строк других типов. Поле автоматически добавляется к нужным типам, значения сохраняются.

## [1.8.2] — 2026-05-13

### Fixed
- В реестре не отображались дополнительные колонки, даже если у типа документа были настроены поля. Схема дополнительных полей теперь возвращается в ответе и колонки становятся доступными.

## [1.8.1] — 2026-05-13

### Fixed
- Импорт ошибочно сообщал об отсутствии типа документа на пустых строках-разделителях между группами компаний. Такие строки пропускаются без ошибки.

## [1.8.0] — 2026-05-13

### Added
- Опция «Применить ко всем типам документов в этом импорте» для нового дополнительного поля — поле добавляется ко всем типам, присутствующим в файле, и значения не теряются.

### Changed
- В запросе подтверждения импорта добавлен флаг применения поля ко всем типам (по умолчанию выключен).

## [1.7.4] — 2026-05-13

### Fixed
- Создание нескольких новых полей с кириллическими названиями при импорте приводило к перезаписи их друг другом. Названия теперь транслитерируются в уникальные ключи.
- Пустые строки-разделители при импорте больше не вызывают ошибку «Не указан тип документа».

### Added
- Подсказка при выборе типа документа для нового поля: поле создаётся только на выбранном типе.

## [1.7.3] — 2026-05-13

### Fixed
- Импорт реестра показывал «0 строк» на корпоративных файлах с английскими названиями типов и объединёнными ячейками. Добавлены поиск типа по отображаемому имени, заполнение объединённых ячеек и понятные сообщения об ошибках строк.

## [1.7.2] — 2026-05-13

### Fixed
- Таблица подписок на календарь была сжата на широких экранах. Контейнер расширен, заголовки и значения колонок больше не переносятся.

## [1.7.1] — 2026-05-13

### Fixed
- На странице подписок на календарь не отображались ФИО и email. Данные подтягиваются по пользователям, диалог удаления показывает корректную подпись.

### Added
- Колонка «Доступ к календарю» со статусом-индикатором и раскрывающаяся инструкция (команда для IT и шаги для пользователя в Outlook).

## [1.7.0] — 2026-05-13

### Added
- Календарный пайплайн заработал полностью: события документов синхронизируются в общий календарь Exchange. Добавлены фоновые обработчики для рассылки событий и синхронизации календаря.

### Fixed
- Исправлен ряд ошибок в обработке событий и обращениях к реестру, из-за которых события не доходили до календаря.

## [1.6.1] — 2026-05-12

### Fixed
- Исправлены ошибки интеграции автоматической выдачи прав на календарь через Exchange (EWS). При отказе Exchange в правах пользователю показывается понятное сообщение с готовой командой для IT.

## [1.6.0] — 2026-05-12

### Added
- Автоматическая выдача прав на общий календарь Exchange (уровень «Reviewer») при добавлении пользователя в подписки, без участия IT. Добавлены статус выдачи, повторная попытка и отзыв прав.

## [1.5.4] — 2026-05-12

### Changed
- Результат теста канала email теперь сообщает, через какой транспорт письмо доставлено (корпоративный Exchange или SMTP), без упоминаний dev-инструментов.

## [1.5.3] — 2026-05-12

### Fixed
- Тест канала email отправлял письмо не на адрес текущего администратора. Адрес получателя теперь берётся из актуальных данных, токен обновляется сразу после смены email.

## [1.5.2] — 2026-05-12

### Fixed
- Email-канал не работал на корпоративном Exchange из-за запрета SMTP Send-As. Добавлен автоматический резервный путь отправки через Exchange (EWS).

## [1.5.1] — 2026-05-12

### Fixed
- При смене email вместо понятного сообщения «обратитесь к администратору» показывалась общая ошибка подключения. Коды ошибок корректно пробрасываются на интерфейс.

## [1.5.0] — 2026-05-12

### Added
- Самостоятельная смена email в профиле: двухшаговый проверенный процесс с подтверждением TOTP и кодом из нового почтового ящика.
- Внутренний API для отправки транзакционных писем (используется при смене email).

### Security
- Код подтверждения приходит только на новый адрес; перехвативший сессию не сможет завершить смену без доступа к целевому ящику.
- Код хранится в хешированном виде, число попыток ограничено, в логах и ответах API код не появляется.

## [1.4.8] — 2026-05-12

### Fixed
- Диалоги редактирования каналов теперь подгружают текущие настройки, а секреты можно оставить без изменений. Ошибка об обязательном секрете показывается под нужным полем.

## [1.4.7] — 2026-05-12

### Fixed
- Несколько исправлений отправки email на корпоративном Exchange: автоопределение TLS по порту, пропуск авторизации при пустых учётных данных, явное указание отправителя и логирование ответа SMTP для диагностики.

## [1.4.6] — 2026-05-12

### Fixed
- Диалог редактирования канала открывался с пустыми полями. Добавлен запрос текущей конфигурации (секреты маскируются) и частичное обновление секретов — менять заново всё необязательно.

## [1.4.5] — 2026-05-12

### Fixed
- Кнопка «Тест» в настройках каналов мгновенно закрывала диалог и теряла результат. Результат теста теперь показывается прямо в диалоге с понятным текстом и кнопками «Повторить» / «Готово».

## [1.4.4] — 2026-05-12

### Fixed
- Длинные диалоги выходили за пределы экрана, скрывая кнопки. Все диалоги ограничены по высоте и прокручиваются внутри.

## [1.4.3] — 2026-05-12

### Fixed
- Тест канала email возвращал внутреннюю ошибку без объяснения при долгом ответе сервера. Таймаут увеличен, при его превышении показывается понятное сообщение на русском.

## [1.4.2] — 2026-05-12

### Fixed
- Импорт xlsx падал из-за несовпадения имени поля с примерами значений между сервером и интерфейсом. Поле приведено к каноническому имени.

## [1.4.1] — 2026-05-12

### Fixed
- После обновления зависимостей каждый запрос к API возвращал ошибку авторизации (не проверялась подпись токенов). Недостающая зависимость добавлена явно.

## [1.4.0] — 2026-05-12

### Changed
- Время жизни сессии изменено с 7 дней на 12 часов. Действующие сессии сохраняют прежний срок до следующего полного входа.

### Fixed
- Устранены самопроизвольные разлогины: гонка одновременного обновления токена в нескольких вкладках, сброс cookie на HTTP и рассогласование настроек cookie между сервисами. Параметры cookie и времени жизни токенов теперь настраиваются.

## [1.3.1] — 2026-05-12

### Fixed
- Версия в футере при разработке обновляется автоматически после изменения версии проекта.

## [1.3.0] — 2026-05-08

### Added
- Дополнительные поля под каждый тип документа (текст, число, дата, перечисление), настраиваемые администратором.
- Умный двухшаговый импорт xlsx: предпросмотр с автоопределением типов колонок, затем подтверждение с решениями по каждой колонке (создать поле, переименовать, сопоставить, пропустить).
- Динамические колонки реестра по дополнительным полям и проверка значений при сохранении документа.

### Changed
- Прежний одношаговый импорт продолжает работать для файлов без новых колонок; при наличии новых колонок предлагается двухшаговый процесс.

## [1.2.1] — 2026-05-08

### Fixed
- Набор исправлений после выпуска интеграции с Exchange: совместимость драйвера календаря с библиотекой Exchange, режим доступа по умолчанию и устранение ошибок при повторных вызовах.
- Исправлена авторизация на страницах подписок на календарь и раздела «Система» (запросы уходили без токена).

### Changed
- Футер с версией выводится один раз на уровне корня и виден на каждой странице, включая страницы входа и 404.

## [1.2.0] — 2026-05-08

### Added
- Роль «супер-администратор» и раздел «Система» со строгим разделением доступа: супер-администратор видит только `/system/*`, обычный администратор — реестр и `/admin/*`.
- Отдельный сервис управления системой с жёстким белым списком разрешённых действий и доступом только из внутренней сети.
- Страницы раздела «Система»: состояние, очереди, миграции, ключи, логи, аудит и обслуживание. Изменяющие операции требуют TOTP, разрушительные — дополнительного ввода-подтверждения.
- Защита от удаления последнего супер-администратора.

## [1.1.0] — 2026-05-08

### Added
- Два новых канала уведомлений: общий календарь Microsoft Exchange (через EWS) и ICS-лента (iCalendar по ссылке, работает даже при недоступности Exchange).
- Синхронизация документов с календарём: отдельные события на каждый срок предуведомления и на день истечения; страница управления подписками на календарь.

## [1.0.0] — 2026-05-07

### Added
- Базовый выпуск платформы Лоцман.
- Пять backend-сервисов на FastAPI с чистой архитектурой: `auth-service`, `registry-service`, `notification-service`, `audit-service` и шлюз `web-bff`; общее ядро `lotsman-shared`.
- PostgreSQL 16 (схемы `auth`, `registry`, `notification`, `audit` с раздельными ролями и outbox-таблицами) и Redis 7; развёртывание через Docker Compose.
- SPA на React 19 (TanStack, Tailwind, shadcn/ui) с локализацией RU/EN.
- Авторизация: пароли argon2id, обязательный TOTP, access JWT (15 минут) и refresh-токен (cookie), резервные коды, блокировка при переборе, RBAC (admin / editor / viewer).
- Реестр документов: контрагенты, типы документов, документы, вложения, экспорт в xlsx; виртуализированная таблица в стиле Excel с закреплённой колонкой «Контрагент», поиском, inline-редактированием и карточкой документа.
- Уведомления: каналы email/Telegram/Dion с конфигурацией в БД (секреты шифруются), приглашение пользователей и политика «не менее двух администраторов».
- Append-only журнал аудита с раздельными ролями БД.
- Документация: ADR-0001..ADR-0004, требования и руководства пользователя (RU).

[Unreleased]: https://github.com/MaximilianKaufmannCode/lotsman/compare/v1.29.6...HEAD
